{% extends "templates/base.peb" %}

{% block content %}
{% if resultado.foiErro %}
<h1> {{ resultado.getMsg }}</h1>
{% else %}

<ul>
    {% for lanche in lanches %}
    <li>{{lanche.nome}}, R${{lanche.valor}}</li>
    {% endfor %}
</ul>
{% endif %}
<button><a href="/">Voltar</a></button>

{% endblock %}